---
"@fn-sphere/core": minor
---

Add sorting functionality with `createSorterSphere` and utilities. Provides schema-driven, multi-field sorting for Zod-typed data with customizable comparison functions and a built-in `presetSort` for strings, numbers, booleans, dates, and enums.
