---
"@fn-sphere/core": minor
"@fn-sphere/filter": minor
---

Add support for Zod enum types in filter predicates and UI components. The `equals`, `notEqual`, `contains`, and `notContains` functions now work with `z.enum()` schemas. Added enum and enum array data input views for selecting enum values in the filter UI.
